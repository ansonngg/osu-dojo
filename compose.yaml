services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5051:5051"

  frontend:
    build:
      context: ./Web/ClientApp
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      API_URL: http://api:5051
